{
  "name": "apitests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test tests/api/",
    "test:positive": "npx playwright test --grep @positive tests/api/",
    "test:negative": "npx playwright test --grep @negative tests/api/",
    "test:report": "npx playwright show-report reports"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.52.0"
  }
}
